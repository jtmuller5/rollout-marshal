# Experiments

One line per attempt, appended by the cycle that ran it. **Read this before proposing work
and append before ending a cycle**: it is what stops the next cycle re-running what the
last one already measured.

| date | rung | what changed | local | leaderboard / demo | cycles | keep? |
|---|---|---|---|---|---|---|
| 2026-08-13 | 3 | Two more Google models, each with a job the agent already had (#1091): `gemma-4-31b-it` writes the opening paragraph of the post-action email behind `MARSHAL_SCRIBE`, `gemini-2.5-flash-preview-tts` speaks all twelve narration cues. Both keep a fallback (template, Kokoro) and the mail says which one wrote the words. Measured: Gemma takes 47 to 50s so the timeout is 90 (a 20s one returns `504`); the TTS model allows 3 requests a minute, so `narrate.py` paces at 21s and `--resume` keeps what is already spoken. | 116 tests pass | 0.4 of the 0.6 model bonus claimed; diagrams re-rendered, shot 3 re-recorded, page re-published | 1 | keep |
| 2026-08-13 | 0 | Re-read the playbook's edge list and the prize table against what is built (#1090), looking for a better idea. Found none: Rollout Marshal hits five of the six edges and its weak one, a crowded category, costs nothing, because Individual/Hobbyist and Best Architectural Design are scored, not selected, so one build competes for three prizes. Best Multimodal UX is the only prize it cannot reach. | n/a | no pivot; remaining cycles go to score, not ideas | 1 | keep |
| 2026-08-13 | 4 | `/humanizer` over the five judged-prose files (#1086), the page copy in `rollout_marshal/publish.py` included. Em dashes outside the mermaid blocks and code fences went from 31 to 0. The pass also caught three false numbers: three files claimed 93 tests when pytest reported 106, the README claimed eleven narration cues against twelve, and the page said the closed track "had testers on it" and listed Cloud Run as deployed. | 106 tests pass | both pages re-published | 1 | keep |
| 2026-08-13 | 4 | Made the architecture diagram draw only what is deployed (#1082). Cloud Scheduler, Cloud Run and Secret Manager were ordinary boxes; none of the three is enabled, because the project has no billing account. They are now a dashed "designed, not deployed" group, "What runs where" gains a Deployed? column, and the sequence chart starts at the tick rather than at Cloud Scheduler. | n/a | the picture is on camera for 42s, so a drawn plan was a checkable false claim | 1 | keep |
| 2026-08-13 | 4 | Put the proof of isolation on camera (#1079). Shot 3 was 32s of diagram; it now runs 42s and ends on a panel showing the pytest count and the four `MARSHAL_*` defaults, both read at record time by `shot_data.py` out of the run and out of the `os.environ.get` line that applies each one. A red suite or a default that has become the live edge stops the recording. The ten seconds came out of shots 5 and 6. | n/a | evidence for a whole 30% criterion was absent from the cut; cut still 3:55 against a hard 4:00 | 1 | keep |
| 2026-08-13 | 4 | Published the build write-up, generated from one source (#1078). | n/a | claims 0.2 of the bonus; disclosure sentence included, as the rules require | 1 | keep |
| 2026-08-13 | 0 | Mapped every judging criterion to the file, shot or measurement that answers it (#1073), including the nine parts Stage One passes or fails on. `notes/criteria-map.md`. | n/a | settled that shot 5 needs no Cloud Run deploy: the rules name "Google Cloud Console" first, and Firestore is in it | 1 | keep |
| 2026-08-13 | 4 | Recorded shots 2, 3 and 6 the way shot 4 already was (#1063): real browser on a virtual display, ffmpeg reading that display, nothing composited. `record_shots.sh` takes each length from the shot's window in the script, and the policy on screen in shot 2 is read out of Firestore rather than typed. Also corrected the diagram's "Gemini 3.5 via Vertex AI", which is not what has run: it is the Gemini API key. | n/a | only shots 1 and 5 (Joe's) and a live shot 4 still missing | 1 | keep |
| 2026-08-13 | 2 | Measured the Gemini free tier rather than assuming it (#1060): 20 generate-content requests a day per model on this project, and an ADK tick calls the model once per tool result, so a two-tick take costs about ten. An exhausted day still answers the odd request, so a 200 from one probe proves nothing. `demo/live_alpha.py` and the driver now put the track back when a take dies mid-halt. | n/a | one live take a day, run first thing; rehearse on the fixture brain only | 1 | keep |
| 2026-08-13 | 4 | `demo/assemble.py` fills each of the shooting script's windows with the picture named in `demo/cut.json`, so voice and picture cannot drift (#1060). An unfilmed shot becomes a placeholder card naming who owes it, so the cut stays full length. It refuses a clip shorter than its window and refuses to pad an unedited one at all, and that refusal immediately found the defect it was written for: the live take is 78s against shot 4's 122s window. | n/a | full-length 3:55 rough cut exists with a placeholder at 2:10 | 2 | keep |
| 2026-08-13 | 4 | `demo/narrate.py` speaks the shooting script and lays it on the video's own clock (#1060): a 3:55 bed, matching subtitles, one wav per line. The script is now the timing sheet as well as the prose, so `narrate.py` exits non-zero when a beat holds more words than its window. It did, twice; shot 2 and 4b were shortened rather than the pauses. | tests hold the headings to shot 4's window exactly | narration exists as sound | 1 | keep |
| 2026-08-13 | 4 | Recorded the unedited take instead of describing it (#1060). `demo/record_take.sh` films shot 4 with no camera and no desktop: Xvfb, a real Chromium in kiosk mode, ffmpeg on that display, the real service driving the page. One continuous 78s capture: the agent proposed WIDEN, the gate refused on 41 sessions against a floor of 120, the spike went in, the second tick halted a real Play track with edit `10688190058094298396`. | `tests/test_take.py` forbids seeding or wiping state while wired live | the one thing in this entry that cannot be faked now exists as footage | 1 | keep |
| 2026-08-13 | 4 | Wrote `SUBMISSION.md` and the build write-up (#1053): the Taskmaster category, the three mandatory technologies, the five submission parts. | n/a | every submission field has an answer except the video link | 1 | keep |
| 2026-08-13 | 4 | Generated the hosted project page out of the agent's own decision log (#1052). `publish` reads the policy, the rollout and every decision from whatever store the environment points at, and refuses to build a page when the log is empty. Both diagrams are rendered from the README's own mermaid blocks, so the page cannot drift from it. | 50 tests pass (9 new) | hosted URL exists: `http://joemuller.com/rollout-marshal/`, leading with the live halt and Play edit `06187374055212919847` | 1 | keep |
| 2026-08-13 | 4 | Licensed the entry MIT and proved the clean-clone spin-up (#1051) from an unauthenticated clone: `uv venv && uv pip install -r requirements-dev.txt`, then tests green and `bash demo/run_demo.sh` exit 0 with no credential. | n/a | repo public at `github.com/jtmuller5/rollout-marshal`; every push is an outward action from here on | 1 | keep |
| 2026-08-13 | 2 | Covered the demo path with tests (#1050): the four operator commands, the FastAPI service the demo is filmed through, and `run_demo.sh` itself. Two of them guard the recording rather than the code, by requiring the spoken numbers to still appear in `notes/demo-script.md`. `conftest` pins every variable that selects a live edge, SMTP included. | 41 tests pass (18 new), about 2s | a shell left over from a live Play run can no longer make the suite write to a real account | 1 | keep |
| 2026-08-13 | 1 | Wrote the shooting script as word-for-word prose under headings carrying the times (#1046), so it doubles as the timing sheet everything later reads. | n/a | the cut, the narration and the tests all key off this one file | 1 | keep |
| 2026-08-13 | 2 | Ran the spine on real Firestore and then halted a real rollout from the agent itself (#1036). Firestore is live on `gen-lang-client-0325469250`, free tier, billing disabled, with the `(app ASC, ts DESC)` index the decision log needs. One tick with `MARSHAL_PLAY=live`, `MARSHAL_STORE=firestore`, `MARSHAL_BRAIN=adk` took Bakedown's `alpha` release to halted, committing Play edit `06187374055212919847`. Measured: 3s for the write, 58s for the tick. | n/a | the demo's central action is the agent's, not a script's; Cloud Run, Cloud Build, Artifact Registry and Cloud Scheduler all refuse to enable without billing | 2 | keep |
| 2026-08-13 | 2 | Built the spine to shot 4 (#1034): service, ADK agent, gate, executor, Play client, crash feed, store, email, streamed log, CLI, 23 tests. `bash demo/run_demo.sh` runs refusal → spike → halt → email on one command. Ran it on the real Gemini 3.5 Flash: the model proposed WIDEN, took the gate's session-floor refusal, held, then halted on the spike. Live Play client verified read-only against the real `alpha` track. | 23 tests pass; demo green on both brains | shot 4 exists outside the fixture Play client; nothing deployed | 1 | keep |
| 2026-08-13 | 2 | Proved the Play WRITE path (#1032): stage, halt, resume-and-widen, clear, all committed on Bakedown's `alpha` closed testing track with no testers. 5 committed edits, raw bodies in `notes/play-write-path.md`. Two Play refusals found: a track's first release cannot be staged, and a release cannot be removed from a track by API. | n/a | the demo's central action is real, not a mock | 1 | keep |
| 2026-08-13 | 1 | Wrote the four-minute shot list before any code: halt path as the centrepiece, gate refusal before the halt, one unedited take. Rung 0 criteria weights read off the rules page and written into the project prompt. | n/a | shot list exists; no footage yet | 1 | keep |
