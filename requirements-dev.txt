# Everything in requirements.txt, plus what the test suite needs and the runtime does not.
# httpx is already pulled in by google-genai; fastapi's TestClient needs it, so it is pinned
# here as well rather than left to a transitive resolution.
-r requirements.txt
pytest==9.1.1
httpx==0.28.1
